__pycache__/
*.py[cod]
.venv/
venv/
.DS_Store
# generated benchmark output; results/dashboard.png is checked in for the README
results/*.csv
results/*.html
# raw playback traces: megabytes of frames, regenerated by tools/twin.py --trace
results/trace_*.json
# sweep output: regenerate with tools/twin.py --sweep; the committed compare_*
# and seed_* files are the numbers quoted in the READMEs
results/sweep_*

# Fuel model cache populated by tools/fetch_world_assets.py. The derived
# layout in config/ is committed instead; the upstream meshes are not ours
# to redistribute.
.fuel-cache/

# colcon build products
ros2_ws/build/
ros2_ws/install/
ros2_ws/log/
